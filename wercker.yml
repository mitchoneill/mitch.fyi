box: debian
build:
  steps:
    - install-packages:
        packages: git
    - script:
        name: download theme
        code: |
            $(git clone https://github.com/gcushen/hugo-academic ./themes/academic)
    - arjen/hugo-build:
        theme: academic
        flags: --buildDrafts=true